# Build outputs
windows/build/
windows/packages/
linux/build*/

# Vendored prebuilt Velopack C/C++ library (fetched via linux/fetch-velopack.sh)
linux/third_party/

# Temporary scripts and artifacts
temp/

# Large ONNX model weights (distributed via GitHub Release asset, see README)
windows/models/onnx/model_quantized.onnx_data

# Test artifacts
third_party_tests/.logs/
third_party_tests/.env
third_party_tests/out.txt
__pycache__/
*.pyc
.pytest_cache/

# Wrangler local state (Cloudflare account cache, local dev)
cloudflare/.wrangler/
cloudflare/.dev.vars

# Store submission content (kept out of the repo)
windows/store-listing/

# Internal planning docs (not for public release)
docs/linux-port-plan.md

# Translation/CI tooling venv (lrelease, deep-translator)
.transvenv/

# Sparse-attention re-export scratch (cloned source repo, intermediate models)
_sparse_export/
